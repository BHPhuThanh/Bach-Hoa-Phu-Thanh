/**
 * Shell bán hàng (tab POS) — cùng `App.jsx`. Thanh tìm & gợi ý dùng `resolvePosSuggestCatalog` /
 * `catalogSearchSimple.js`; log đồng bộ: `[SYSTEM-WIDE-SEARCH-V9]` (pos-resolvePosSuggestCatalog, pos-scanPosTextMatches, …).
 */
export { default } from './App.jsx'
