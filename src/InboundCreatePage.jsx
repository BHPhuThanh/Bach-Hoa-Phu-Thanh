import App from './App.jsx'

/** Route `/nhap-hang/tao-moi` — shell App + Hub mở sẵn form phiếu nhập (IndexedDB, Chọn nhanh, F3). */
export default function InboundCreatePage() {
  return <App standaloneInboundCreate />
}
